#include<stdio.h>
int main()
{
    int i,a[10],s=0;
    printf("enter 5 elements of array");
    for(i=0;i<5;i++)
    {
       scanf("%d",&a[i]);
    }
    i=0;
    for(i=0;i<5;i++)
    {
        s=s+a[i];
    }

    printf("%d",s);
}
