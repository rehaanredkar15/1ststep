#include<stdio.h>
#include<string.h>
int main()
{
    char a[10],b[10];
    printf("enter the first string\n");
    gets(a);
    printf("enter the second string\n");
    gets(b);
    strcat(a,b);
    printf("the connected string is   %s\n",a);
    return 0;
}
