#include<stdio.h>
int main()
{
    int a,b,*p,*q,mul;
    printf("enter first no");
    scanf("%d",&a);
    printf("enter second no");
    scanf("%d",&b);
     p=&a;
     q=&b;
    mul=*p**q;
    printf("%d is the multiplication",mul);
    return 0;

}
